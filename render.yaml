services:
  - type: web
    name: raas-api
    runtime: python
    buildCommand: pip install -r agent/requirements.txt
    startCommand: cd agent && uvicorn raas_agent:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
